# Poll Bot
Discord bot to create polls in the channel

Utilizes Node.js and Discord.js

# Usage
You can run the bot locally, you just have to update the token on the last line of index.js

This was designed to run on Heroku

# Commands
`$poll:create` creates a poll

`$poll:channel` set the poll channel
